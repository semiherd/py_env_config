# handling env-variables from a configuration class defined seperately

A Config Class is created to manage environment variables with different conditions as required/optional or of different type conversion or type validation on the app-level.
